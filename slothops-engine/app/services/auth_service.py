"""Auth service.

Higher-level helpers built on top of auth.py — signup orchestration,
workspace bootstrapping, session lookup. Routers in app.api.auth
delegate here so the route handlers stay thin.
"""
